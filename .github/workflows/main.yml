
name: Make and Test

on: [push]

jobs:
  build:
    name: Make and Test
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v2
      - name: make
        run: make
      - name: make test
        run: make test
